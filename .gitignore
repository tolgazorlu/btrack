# Binary at repo root only — do NOT match skills/btrack/, .claude/skills/btrack/,
# or cmd/skill_data/btrack/ which are tracked source.
/btrack
/btrack.exe
dist/
*.test
*.out
.env
node_modules
/.next
/.source
# macOS finder metadata — leaks into skills/ and breaks the embedded tree check
.DS_Store
